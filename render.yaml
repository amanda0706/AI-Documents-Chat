services:
  - type: web
    name: luminaclause-api
    runtime: docker
    rootDir: backend
    dockerContext: .
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: ANALYSIS_PROVIDER
        value: local
